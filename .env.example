# Base URL of your backend API (no trailing slash).
NEXT_PUBLIC_API_URL=https://demo.webwideit.solutions/pos-aishwarya/public

# Optional: tax rate percentage used in POS (default 10)
# NEXT_PUBLIC_TAX_RATE=10
